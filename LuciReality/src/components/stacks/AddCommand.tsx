import React from "react";
import { SafeAreaView } from "react-native-safe-area-context";

export const AddCommandScreen: React.FC = () => {
    return (
        <SafeAreaView>
            
        </SafeAreaView>
    )
}